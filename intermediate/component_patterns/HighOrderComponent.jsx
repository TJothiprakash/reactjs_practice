// The primary purpose of HOCs is to share logic between React components. Instead of duplicating code across multiple components, you can encapsulate the common logic within an HOC and then apply that HOC to the components that need it.

